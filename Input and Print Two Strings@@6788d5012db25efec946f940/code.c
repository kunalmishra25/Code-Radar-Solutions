#include <stdio.h>
#include <stdio.h>

int main() {
    char str1[50];
    char str2[50];
    scanf("%s",str1);
    scanf("%s",str2);
    printf("You entered: %s and %s",str1,str2);


    return 0;
}